# สรุปภาพรวมสถาปัตยกรรมและการทำงานของระบบ Face Tracking System

> [!NOTE]
> เอกสารฉบับนี้รวบรวมสรุปโครงสร้างการทำงาน โมเดลปัญญาประดิษฐ์ (AI Models) ที่ใช้ในระบบ การตรวจจับเหตุการณ์ผิดปกติ (Security Violations) และระบบบริหารจัดการสถานะการขาดการเชื่อมต่อ (Disconnection Handling) ของระบบ **Face Tracking System**

---

## 1. โมเดลปัญญาประดิษฐ์และคอมพิวเตอร์วิทัศน์ที่ใช้ในระบบ (AI & Machine Learning Models)

| ชื่อโมเดล / เทคโนโลยี | สถาปัตยกรรม (Architecture) | หน้าที่และการทำงานในโปรเจค |
| :--- | :--- | :--- |
| **MediaPipe FaceLandmarker** | Google TensorFlow Lite (Client WASM) | **ตรวจจับ 468 จุด 3D Mesh + 10 จุดม่านตา:** ใช้เป็น Engine หลักในการคำนวณองศาใบหน้าเรียลไทม์ (Yaw/Pitch) ความเร็วสูง 60 FPS เพื่อติดตามทิศทางการมองของผู้สอบ |
| **face-api.js Models** | MobileNet V1 + ResNet-34 | **พิสูจน์ตัวตน & ยืนยันใบหน้า (Face 2FA):** ประกอบด้วย SSD MobileNet V1 (หาตำแหน่งใบหน้า), FaceLandmark68Net (วัดการกะพริบตา EAR) และ FaceRecognitionNet สกัด 128-D Feature Vector ยืนยันตัวตนก่อนเข้าสอบ |
| **YOLOv8-Face** | YOLOv8 Quantized (ONNX Runtime) | **ตรวจจับกรอบใบหน้าความเร็วสูง:** ตรวจจับกรอบใบหน้าและ 5 จุดแลนด์มาร์คหลัก สำหรับนำมาเปรียบเทียบในตาราง Multi-Engine Benchmark |
| **Dlib 68-Point Model** | HOG (Histogram of Oriented Gradients) + SVM | **สกัดพิกัดแลนด์มาร์คมาตรฐาน 68 จุด (2D):** ใช้เป็นเกณฑ์อ้างอิงมาตรฐานในการวัดความแม่นยำร่วมกับ Engine อื่นๆ |
| **OpenFace Models** | Constrained Local Model (CLM) & FACS | **วิเคราะห์กล้ามเนื้อใบหน้าและสายตา 3D:** สกัดค่า Action Units (AUs) เช่น AU12 (ยิ้ม), AU45 (กะพริบตา) และคำนวณทิศทางการมอง (Gaze Vector) |
| **InsightFace / ArcFace** | ResNet/MobileFaceNet (Margin Loss) | **สกัด Biometric Feature 512-D:** ใช้ยืนยันตัวตนเชิงลึกเพื่อป้องกันการสวมสิทธิ์สอบ (Impersonation Protection) |
| **MiniFASNet** | FeatherNets Anti-Spoofing | **ตรวจจับตัวจริง/ภาพหลอก (Liveness Detection):** แยกแยะคนจริงในกล้อง ป้องกันการนำรูปถ่าย สมาร์ทโฟน หรือวิดีโอมาเปิดหลอกกล้อง |
| **L2CS-Net** | ResNet Gaze Angle Regression | **ทำนายองศาการมองของดวงตา (3D Gaze Angle):** คำนวณทิศทางสายตาจากภาพครอปใบหน้าโดยเฉพาะ |

---

## 2. การตรวจจับเหตุการณ์ผิดปกติทางการสอบ (Security Violations & Risk Events)

> [!IMPORTANT]
> ระบบมีการตรวจจับพฤติกรรมเสี่ยงแบบเรียลไทม์ พร้อมบันทึกสถิติและแจ้งเตือนในระบบติดตาม โดยแบ่งเป็น 5 รูปแบบหลัก:

1. **ตรวจพบหลายใบหน้าในกล้อง (Multiple Faces Detection):**
   * ตรวจจับเมื่อพบใบหน้ามากกว่า 1 ใบหน้าขึ้นไปในกล้อง (`count > 1`) เพื่อป้องกันการมีผู้อื่นแอบนั่งข้างๆ หรือช่วยตอบคำถาม
2. **การหายไปของใบหน้า (Loss of Face Detection):**
   * บันทึกเมื่อผู้สอบหลุดออกจากกรอบกล้อง หรือก้มหลบกล้องเกินเกณฑ์เวลาที่กำหนด พร้อมบันทึกจำนวนครั้งและระยะเวลารวม
3. **การหันศีรษะออกจากหน้าจอ (Looking Away Detection):**
   * ตรวจจับเมื่อผู้สอบหันไปทิศทางอื่นเกินเกณฑ์องศา ได้แก่ `LEFT` (หันซ้าย), `RIGHT` (หันขวา), `UP` (เงยหน้า), และ `DOWN` (ก้มหน้า)
4. **ระยะห่างจากหน้าจอเกินกำหนด (Excessive Distance Detection):**
   * คำนวณระยะห่างระหว่างผู้สอบกับกล้องเป็นเซนติเมตร (cm) แบบเรียลไทม์ และแจ้งเตือนเมื่อถอยห่างเกิน 80 ซม. (`distance > 80cm`)
5. **ใบหน้าไม่ตรงกับผู้สมัครสอบ (Face Verification Mismatch):**
   * เปรียบเทียบโครงสร้างใบหน้าขณะสอบกับรูปที่ลงทะเบียนไว้ เพื่อป้องกันการสวมสิทธิ์สอบ

---

## 3. ระบบจัดการสถานะ "ขาดการเชื่อมต่อ" (Disconnection Handling System)

> [!TIP]
> ระบบมีกลไกป้องกันข้อมูลสูญหายและการจัดการสถานะเซสชันอัตโนมัติ ทั้งในฝั่ง Client และ Server:

- **การดักจับฝั่ง Client (Real-Time Disconnection Flushing):**
  - ระบบเพิ่ม Event Listener ดักจับการปิดหน้าต่างเบราว์เซอร์ หรือการเปลี่ยนแท็บ (`window.onbeforeunload`, `document.onvisibilitychange`)
  - หากผู้ใช้ปิดแท็บหรือหลุดจากระบบกะทันหัน ระบบจะส่งข้อมูลล่าสุดด้วย HTTP Keep-Alive / Beacon API เพื่ออัปเดตสถานะเซสชันเป็น **`DISCONNECTED`** (ขาดการเชื่อมต่อ) หรือ **`INTERRUPTED`** (หยุดการบันทึกกลางคัน) ทันที
- **ระบบทำความสะอาดเซสชันค้างบน Server (Server-Side Auto-Close Cleanup):**
  - มีระบบหลังบ้าน `autoCloseStaleSessions()` ทำหน้าที่ตรวจสอบเซสชันที่มีสถานะ `IN_PROGRESS` (กำลังดำเนินการ) 
  - หากไม่มีการส่งสัญญาณการตรวจจับเข้ามาเกิน **60 วินาที** (Stale Session) ระบบจะทำการคำนวณเวลาสถิติล่าสุด และอัปเดตสถานะเซสชันเป็น **`DISCONNECTED`** ให้อัตโนมัติ เพื่อไม่ให้เซสชันค้างอยู่ในระบบ
